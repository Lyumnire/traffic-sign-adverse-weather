# 恶劣天气下的交通标识识别 — 睿抗机器人开发者大赛(raicom)智海算法调优赛国赛复盘

> **赛事成绩**：
> - **国赛**：F1 Macro = **0.945**，**二等奖**
> - **初赛**：F1 Macro = **0.922**，**二等奖**
> 
> 本文档全面系统地复盘整个比赛周期的技术路线、演进过程、经验教训与踩坑实录，将比赛成果沉淀为可在后续 CV 竞赛与工程交付中直接复用的技术体系。

---

## 目录
1. [赛题深度解析与环境挑战](#一赛题深度解析与环境挑战)
2. [版本迭代轨迹：从 0.922 到 0.945](#二版本迭代轨迹从-0922-到-0945)
3. [核心技术架构与决胜策略](#三核心技术架构与决胜策略)
4. [数据工程与训练调优体系](#四数据工程与训练调优体系)
5. [四大实战避坑实录与根因剖析](#五四大实战避坑实录与根因剖析)
6. [可复用的算法竞赛工程化方法论](#六可复用的算法竞赛工程化方法论)
7. [未来演进与未竟探索](#七未来演进与未竟探索)

---

## 一、赛题深度解析与环境挑战

### 1.1 赛题属性与评估指标
- **任务目标**：图像级单标签分类，将输入的任意恶劣天气图像分类为 **25 类交通标志**之一。
- **评估指标**：**Macro-F1 Score**（所有类别的 F1 分数算术平均值）：
  $$\text{Macro-F1} = \frac{1}{N} \sum_{i=1}^{N} \frac{2 \times P_i \times R_i}{P_i + R_i}$$
  **指标关键特性**：Macro-F1 对长尾分布极其敏感。样本极少的少数类一旦出现假阴性（漏检）或假阳性（误判），该类的 F1 将剧烈下跌，直接拉垮总成绩。因此，**方案必须兼顾每一个细分类别，绝不能为了大类准确率而牺牲少数类召回率**。

### 1.2 恶劣天气复杂退化特征
数据集采集自真实车载摄像头与行车记录仪，存在多维重叠退化：
1. **天气退化**：强降雨水雾、雨滴挂壁、路面反光；强降雪与积雪遮盖；浓雾与雾霾导致能见度极低、对比度骤降。
2. **光照退化**：夜间极度弱光、对面车灯强眩光（Overexposure）、路灯阴影交替。
3. **成像退化**：高速行车引起的运动模糊（Motion Blur）、摄像头镜头污损、图像 JPEG 压缩失真与块效应。
4. **物理退化**：标志部分被树木/建筑物遮挡、老化掉漆。

### 1.3 算力与平台的苛刻约束（决胜关键瓶颈）
- **推理平台**：MoModel Linux 评测机（Jupyter/Notebook 镜像环境）。
- **硬件配置**：**仅 2 核 CPU + 8GB RAM，无 GPU 加速**。
- **运行上限**：测试集约 750~800 张，平台设定推理总时长上限为 **70 分钟**。
  - 单张图片全链路（加载 + 预处理 + 多模型前向 + 集成后处理）总预算仅约 **50~80ms**。
- **黑盒评测机制**：平台线上运行不输出标准输出日志，只返回一个浮点数 Macro-F1 或报超时/报错。**一旦代码内部静默抛出异常或模型输出常数，选手将面对“零日志调试”的绝境**。

---

## 二、版本迭代轨迹：从 0.922 到 0.945

从初赛到国赛，团队经历了多次架构重构与持续精调，形成了清晰的成绩跃迁链条：

```
初赛 (F1 = 0.922)
  └─ 天气分类三模型（ConvNeXt-Tiny + MobileNetV3 + EffNet-B2）+ 双解码头 + 水平翻转 TTA
       │
       ▼ [进入国赛：类别扩展到 25 类细粒度标志，算力约束放大]
       │
国赛 v1 (F1 = 0.929) - [0.929.py]
  ├─ 引入大容量三模型组合 (ConvNeXt V2-Base + Swin V2-Base + EfficientNetV2-M)
  ├─ 移除水平翻转 TTA，改用多尺度 TTA
  └─ 暴露问题：平台缺失 timm 导致 ConvNeXt V2 架构隐性降级，得分受限
       │
       ▼ [修复依赖与架构校验]
       │
国赛 v3 (F1 = 0.9418) - [0.9418.py]
  ├─ 增加自动检测/安装 timm 逻辑，保障 ConvNeXt V2 权重完美加载
  ├─ 去除本地特化调优超参，采用稳健的等权软投票 (Equal-Weight Ensemble)
  └─ 成绩直接突破至 0.9418
       │
       ▼ [加权集成与 CPU 算力极致优化]
       │
国赛 v4 终版 (F1 = 0.945) - [0.945.py] 🏆 国赛二等奖
  ├─ 验证集深度分析：发现 ConvNeXt V2 单模达到 0.938，显著高于 Swin (0.916) 和 EffNet (0.917)
  ├─ 实施差异化加权软投票 (Weighted Soft Voting: 0.60 / 0.20 / 0.20)
  ├─ 移除 PyTorch JIT 预编译（排查发现 JIT 在 2 核 CPU 上反而增加内存复制与开销）
  └─ 取得 0.945 最终高分，稳固斩获全国二等奖！
```

---

## 三、核心技术架构与决胜策略

### 3.1 异构“三剑客”模型选型矩阵
为在 2 核 CPU 极其有限的算力下压榨出极致精度，团队摒弃了低容量的小模型，并精准避开了高算力开销的陷阱：

| 模型 | 来源与规格 | 参数量 | 推理分辨率 | CPU 计算特性 | 在集成中的战略定位 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **ConvNeXt V2-Base** | `timm: convnextv2_base` | 89M | 384×384 | 纯卷积，高度适配 CPU MKL/OpenBLAS 加速 | **精度核心（单模 F1=0.938）**：FCMAE 自监督预训练带来绝佳的局部与纹理提取能力 |
| **Swin Transformer V2-Base** | `torchvision: swin_v2_b` | 88M | **256×256** | 窗口局部注意力（Shifted Window），开销可控 | **多样性保障（单模 F1=0.916）**：基于 Transformer 的全局上下文表征，与 CNN 形成正交互补 |
| **EfficientNetV2-M** | `torchvision: efficientnet_v2_m` | 54M | 384×384 | 渐进式 Fused-MBConv 结构，CPU 吞吐量极高 | **速度与稳定担当（单模 F1=0.917）**：高效抽取跨通道语义，抗过拟合能力强 |

#### 为什么果断放弃 EVA-02 与普通 ViT？
- **ViT 全局自注意力机制在 CPU 上的计算复杂度为 $O(N^2)$**，且涉及海量 `Softmax` 与频繁的张量维度重排（`permute`/`transpose`），缺乏 CPU 底层专用汇编算子加速。
- 实测表明：87M 的 EVA-02-Base 在 2 核 CPU 上处理单张图片耗时高达 **350~500ms**，是同等参数量 ConvNeXt 的近 4 倍！若集成 EVA-02，测试集必超时被判 0 分。
- **策略决断**：仅保留 Swin V2 并将其分辨率降为 256×256，既吸纳了注意力机制的特征互补性，又将 CPU 计算量压制在安全阈值之内。

### 3.2 加权软投票集成（Weighted Soft Voting）
最终版本的 Soft Voting 融合公式：
$$P_{\text{final}} = 0.60 \times P_{\text{ConvNeXtV2}} + 0.20 \times P_{\text{SwinV2}} + 0.20 \times P_{\text{EffNetV2}}$$
$$\hat{y} = \arg\max_c P_{\text{final}}(c)$$
- **原理支撑**：各模型输出经过 `torch.softmax(dim=-1)` 归一化为置信度概率分布。
- **权重推导**：单模验证时，ConvNeXt V2-Base（0.938）展现出对微小字符、模糊边缘的压倒性识别优势；若采用 0.33 等权融合，反倒会被另外两个模型的不确定性拉低。赋予 ConvNeXt V2 **0.60** 的绝对主导权重，辅以两个辅助模型的 **0.20** 投票纠偏，最终实现跨越至 **0.945**。

### 3.3 安全多尺度测试时增强（Safe Multi-Scale TTA）
- **绝不能做水平翻转（Horizontal Flip）**：初赛天气识别是场景级分类，左右翻转无害；但交通标志包含大量**强方向性标志**（如“向左急弯” vs “向右急弯”、“禁止左转” vs “禁止右转”、“靠右行驶” vs “靠左行驶”）。一旦水平翻转，方向完全颠倒，TTA 会直接引入致命噪声！
- **采用安全双尺度 TTA**：
  - 尺度 1：标准输入分辨率（如 384×384）；
  - 尺度 2：轻微扰动尺度（如 320×320 或基于短边保持比例的 Resize + CenterCrop）；
  - 双尺度概率平均，显著平滑了因双三次插值导致的边界混叠效应。

---

## 四、数据工程与训练调优体系

### 4.1 外部数据集之争：域偏移（Domain Shift）的深刻教训
在比赛初期，团队曾尝试引入大型开源交通标志数据集（德国 GTSRB、清华-腾讯 TT100K）：
- **假设**：交通标志种类有限，外部数据量越大，模型泛化越好。
- **实测结果**：直接混入 GTSRB/TT100K 联合训练后，官方验证集 F1 不升反降！
- **根因分析**：
  1. **标注边界与裁剪率差异**：官方数据集的交通标志截取框松紧度、长宽比与公开数据集存在系统性偏差。
  2. **退化分布严重不匹配**：GTSRB 多为晴天顺光拍摄；而本次赛题的核心是恶劣天气（雨、雪、雾霾、夜间反光）。强行灌入干净的外部数据，反而稀释了模型对恶劣天气退化的表征注意力。
- **最终策略**：**放弃外部数据污染，坚持基于 ImageNet-22k/1k 强大先验，直接在官方数据集上端到端训练！**

### 4.2 针对恶劣天气的全流程数据增强
在 `dataset.py` 中，针对赛题特定的 4 类退化，设计了纯 PIL + Torchvision 的轻量高效增强流（无需额外依赖）：
```python
transforms.Compose([
    # 1. 模拟颠簸、倾斜与长宽失真
    transforms.RandomAffine(degrees=8, translate=(0.08, 0.08), scale=(0.9, 1.1)),
    # 2. 模拟夜间低照度、强光眩光与色差退化
    transforms.ColorJitter(brightness=0.3, contrast=0.3, saturation=0.3, hue=0.1),
    # 3. 模拟行车运动模糊、沙尘与浓雾能见度差
    transforms.GaussianBlur(kernel_size=(3, 5), sigma=(0.1, 2.0)),
    transforms.ToTensor(),
    transforms.Normalize(mean=IMAGENET_MEAN, std=IMAGENET_STD),
    # 4. 模拟标志被树叶、雨刷、飞虫遮挡或掉漆
    transforms.RandomErasing(p=0.3, scale=(0.02, 0.2), ratio=(0.5, 2.0), value='random'),
])
```

### 4.3 损失函数与长尾类别治理
- **自动类别加权（Inverse Class Frequency Weighting）**：
  $$w_c = \frac{N_{\text{total}}}{N_c \times C}$$
- **Focal Loss ($\gamma=2.0$) + Label Smoothing (0.1)**：
  - 交通标志在夜间或浓雾下极其难以辨认，常规交叉熵容易被大批“容易分辨的大类标志”主导梯度；
  - Focal Loss 动态降低易分样本权重，迫使模型把关注度集中在恶劣退化下的模糊困难样本；
  - Label Smoothing 抑制模型过拟合到特定光照下的极端特征。

### 4.4 优化器与退火策略
- **优化器**：AdamW（初始 $lr = 2 \times 10^{-4}$，权重衰减 $weight\_decay = 0.05$）；
- **调度器**：前 3 Epoch 线性 Warmup，随后进行 Cosine Annealing 余弦衰减；
- **SWA（Stochastic Weight Averaging）**：在训练最后 10 个 Epoch 开启 SWA，对轨迹上的权重进行算术平均。SWA 帮助权重走出局部陡峭谷底，进入更平坦的极小值区域，显著增强在未知测试集上的泛化能力。

---

## 五、四大实战避坑实录与根因剖析

### 5.1 坑一：ONNX 平台评测 “0.002708 惨案”
- **背景**：为了压榨 CPU 推理速度，团队编写了 `export_onnx.py`，将模型导出为 ONNX，试图利用 ONNX Runtime 的图优化与常量折叠。
- **惨痛现象**：本地导出的 ONNX 与 PyTorch 校验通过；但替换至线上平台后，评测 Macro-F1 暴跌至不可思议的 **0.002708**！
- **数学反推与根因分析**：
  - 赛题一共 25 个类别，$\frac{1}{25} = 0.04$；如果模型对所有图片均预测同一个类（例如全部预测为第 0 类），在极端不平衡的测试集下，Macro-F1 恰好会在 $0.002 \sim 0.003$ 区间！
  - **根本原因**：
    1. **依赖或 ExecutionProvider 异常**：平台 CPU 环境缺失特定 C++ 运行库或版本不匹配，ONNX Runtime 初始化失败或静默抛出异常，进入了外层 fallback 返回默认第 0 类。
    2. **输入张量维度/类型不匹配**：PyTorch 输入为 `(1, 3, H, W)` 且已标准化，而某些 ONNX 算子对通道排列（NCHW vs NHWC）或动态 BatchSize 轴产生断言失败。
    3. **输出未经过 Softmax / 算子丢失**：动态图在静态导出时，Swin V2 中的复杂窗口 roll 操作在低于 opset 16 时被破坏。
- **应急与架构哲学**：
  > **在没有任何报错日志可查的线上黑盒环境中，盲目追求未经验证的技术堆叠是致命的。原生 PyTorch `.pth` 虽稍重，但胜在行为完全确定、环境依赖最小。果断放弃 ONNX 回滚原生 PyTorch，是保住高分奖项的最关键决断！**

### 5.2 坑二：`timm` 第三方库的离线暗礁与压缩包编码陷阱
- **背景**：ConvNeXt V2 依赖 `timm`。但在评测机运行时，环境内并未预装 `timm`。
- **踩坑 1**：在脚本头部写 `os.system("pip install timm")`：
  - 如果平台评测节点在执行任务时切断了外网连接，或者 PyPI 源超时，`pip` 安装静默失败；
  - 导致 ConvNeXt V2 退回到不兼容的 V1 甚至直接 Crash。
- **踩坑 2**：上传 `timm.zip` 供服务器解压：
  - 平台返回：`File Load Error for timm.zip: /home/jovyan/work/timm.zip is not UTF-8 encoded`；
  - 原因：Windows 默认 zip 压缩工具采用 GBK/CP936 编码存储文件名，Linux 解压器以 UTF-8 读取时抛出解码异常。
- **最终杀手锏方案**：
  - 在提交根目录下直接放置已解压的纯源码文件夹 `submit/timm/`；
  - 在 `main.py` 顶端通过 `sys.path.insert(0, str(SUBMIT_DIR))` 优先从本地加载；
  - **彻底摆脱对网络、pip 以及解压工具的全部依赖，做到 100% 离线自给自足！**

### 5.3 坑三：Windows 下 PyTorch DataLoader 多进程静默崩溃
- **现象**：在 Windows 本地运行 `run_train_official.bat` 训练 `efficientnet_v2_m` 时，命令行打印完数据集大小后，立即秒退并跳入下一个模型训练，未留下任何 traceback。
- **排查与根因**：
  - Linux 下 `multiprocessing` 采用 `fork` 机制，而 Windows 强制采用 `spawn`；
  - 在 Windows 下使用 `num_workers > 0` 时，主进程会将 Dataset 对象进行序列化并分发给子进程。如果数据集中包含不可 pickle 的闭包、Lambda 表达式或特定锁，或者子进程加载超大图像导致虚拟内存耗尽，子进程会静默退出；
- **解决**：在本地 Windows 训练脚本中设置 `--num_workers 0`，以单进程流水线运行，瞬间跑通全部 50 轮训练。

### 5.4 坑四：PyTorch TorchScript (JIT) 的负向优化
- **现象**：曾尝试使用 `torch.jit.trace` 编译模型以加速 CPU 推理。
- **实测发现**：在 2 核低配 CPU 上，JIT Trace 不仅没有带来加速，反而导致内存峰值（RSS）暴涨 400MB，且因为动态尺寸分支导致运行耗时增加了 15%。
- **调整**：在 `0.945.py` 中坚决剔除 JIT，采用最稳固轻量的原语：
  ```python
  torch.set_num_threads(2)  # 严格对齐 2 核 CPU，消除线程竞争与上下文切换
  torch.set_grad_enabled(False)
  model.eval()
  ```

---

## 六、可复用的算法竞赛工程化方法论

通过两场比赛的沉淀，团队凝练出一套面向“黑盒算力受限型 CV 竞赛”的通用作战方法论：

```
                    ┌──────────────────────────────────────┐
                    │      算力受限 CV 竞赛决胜方略        │
                    └──────────────────┬───────────────────┘
                                       │
         ┌─────────────────────────────┼─────────────────────────────┐
         ▼                             ▼                             ▼
┌──────────────────┐          ┌──────────────────┐          ┌──────────────────┐
│   模型选型原则   │          │   数据增强法则   │          │   黑盒防御工程   │
├──────────────────┤          ├──────────────────┤          ├──────────────────┤
│• 放弃盲目追求 ViT│          │• 严查物理几何合理│          │• 离线代码全自包含│
│• 优选现代化纯 CNN│          │  性(方向标志禁翻)│          │  (自建离线包目录)│
│• 异构分辨率配比  │          │• 针对性模拟退化  │          │• 回滚机制与基线锁│
│  (384 vs 256)    │          │  优于盲目加外数据│          │• 严格控制线程核数│
│• 强单模加权集成  │          │• Label Smoothing │          │• 预判平台异常回退│
└──────────────────┘          └──────────────────┘          └──────────────────┘
```

1. **强单模加权原则**：不要迷信平均融合。寻找集成中的“主力舰”（如 ConvNeXt V2），赋予其 0.5~0.7 的主导权，将其他互补模型作为纠偏辅助，能够稳定突破单模上限。
2. **物理一致性增强**：永远在增强前思考现实世界的物理规律。识别天气可以翻转，但识别交通标志、字符、医学切片翻转可能改变类别真实标签。
3. **自给自足法则（Self-Contained Rule）**：对于在线评测平台，假设网络必定不可用、系统环境随时重置。所有非标准库依赖（如 `timm`）全部随代码打包。
4. **防御性编程（Defensive Programming）**：在 `predict(img)` 中设计多层 `try-except` 兜底保护，捕获任何异常并安全回退到高置信度类别，永远不要让平台主循环 Crash。

---

## 七、未来演进与未竟探索

若后续算力进一步充裕或开展更深入的工程落地，可从以下方向继续突破：

1. **逐类阈值优化（Class-wise Threshold Search）**：
   - 针对 Macro-F1 指标，通过验证集 Nelder-Mead 算法为每个类别寻找专属的激活阈值，替代传统的统一 `argmax`，可直接带来 0.005~0.01 的 F1 纯增益。
2. **知识蒸馏（Knowledge Distillation）**：
   - 将 ConvNeXt V2-Base + Swin V2-Base + EffNetV2-M 三模型的加权概率作为 Soft Target，蒸馏至单个极其精简的轻量卷积网络（如 MobileNetV4-Conv 或 EdgeNeXt），实现“单模拥有集成精度，同时仅消耗 1/5 CPU 算力”。
3. **去噪/去雾与分类端到端联合训练**：
   - 在主干网络前接入极轻量的无监督去雾/超分适配模块，使模型在进入分类头之前自适应恢复高频边缘特征。

---

> **致谢**：感谢团队成员在比赛期间通宵达旦的研讨、验证与快速迭代！本方案的全部核心代码与复盘文档已整理归档，欢迎开源社区交流与指正。
